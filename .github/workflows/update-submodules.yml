name: Update submodules

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  push-submodule-update:
    runs-on: ubuntu-latest
    strategy:
      matrix: 
        repo:
          - "railreader-grpc-go"
          - "railreader-grpc-swift"
    steps:
      - uses: actions/checkout@v4
        with:
          repository: headblockhead/${{ matrix.repo }}
          submodules: recursive
          path: ${{ matrix.repo }}
      - run: |
          echo "machine github.com login github-actions[bot] password ${{ secrets.RAILREADER_GRPC_GITHUB_TOKEN }}" > ~/.netrc
          chmod 600 ~/.netrc
      - working-directory: ${{ matrix.repo }}
        run: |
          git config user.name 'github-actions[bot]'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git submodule update --remote
          git commit -am "autoupdate railreader-grpc submodule"
          git push
